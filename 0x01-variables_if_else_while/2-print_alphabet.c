#include <stdio.h>
#include <ctype.h>
/**
 * main - Main function prints out the alphabet
 *
 * Return: Returns zero
 */
int main(void)
{
	int m;

	for (m = 'A'; m <= 'Z'; m++)
	{
		putchar(m);
	}
	return (0);
}
