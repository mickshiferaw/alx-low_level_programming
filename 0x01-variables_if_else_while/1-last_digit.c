#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Main code checks the last digit of n
 *
 * Return: Returns zero
 */
int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	if()
	{
		printf("Last digit of %d is");
