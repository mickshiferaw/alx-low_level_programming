#include <stdio.h>
/**
 * main - Main code prints single digits
 * Return: Returns zero
 */
int main(void)
{
	int m;

	for (m = '0'; m <= '9'; m++)
	{putchar(m);
	putchar (',');
	putchar (' ');
	}
	return (0);
}
