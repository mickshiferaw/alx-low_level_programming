#include "lists.h"
#include <stdio.h>

/**
 * print_list - is a function that lists all elements of list_t
 * @h: is a parameter
 * Return: returns zero
 */
size_t print_list(const list_t *h)
{
	return ();
if str == NULL
printf(0);
}
