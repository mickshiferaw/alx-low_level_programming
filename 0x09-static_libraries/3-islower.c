#include "main.h"

/**
 * _islower - Main code checks for lowercase character
 * @c: a character argument
 * Return: Returns 1 if c is lowercase, returns zero otherwise
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
